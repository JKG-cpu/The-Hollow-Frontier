# The Hollow Frontier


# How to run

Install requirements.txt with uv
```bash
uv add -r requirements.txt
# Or with pip
pip install -r requirements.txt
```

Run test.py and a window should open
```bash
uv run test.py
# Or (if you used pip)
python test.py
```
